# B1-1 시스템 관제 자동화 목차

## 이 단계의 목표

B1-1 미션 전체 흐름을 한눈에 확인하고, 어떤 명령을 macOS에서 실행하고 어떤 명령을 OrbStack Ubuntu에서 실행해야 하는지 구분한다.

## 왜 이 작업을 하는가?

이 미션은 단순히 Bash 파일 하나를 만드는 과제가 아니다. SSH, 방화벽, 사용자/그룹, 디렉터리 권한, 앱 실행, 관제 로그, cron, logrotate까지 운영 흐름 전체를 증빙해야 한다.

## 사전 확인

macOS 프로젝트 폴더에서 확인한다.

```bash
pwd
ls -la
git status
```

OrbStack Ubuntu에서는 다음을 확인한다.

```bash
cat /etc/os-release
uname -m
whoami
id
```

## 실행 명령어

이 문서는 실행 명령이 아니라 전체 순서 안내다.

1. `docs/01-환경준비.md`
2. `docs/02-agent-app-압축해제와-배치.md`
3. `docs/03-계정-그룹-생성.md`
4. `docs/04-디렉토리-권한-설정.md`
5. `docs/05-SSH-방화벽-설정.md`
6. `docs/06-환경변수-키파일-설정.md`
7. `docs/07-agent-app-실행-검증.md`
8. `docs/08-monitor-sh-구현-검증.md`
9. `docs/09-cron-자동실행.md`
10. `docs/10-logrotate-로그용량관리.md`
11. `docs/11-보너스-report-sh.md`
12. `docs/12-보너스-로그-아카이브.md`
13. `docs/13-전체-검증-체크리스트.md`
14. `docs/14-트러블슈팅.md`
15. `docs/15-동료평가-대비-질문답변.md`

## 명령어 설명

- macOS: Git, 문서 편집, ZIP 파일 이동, 제출 준비를 담당한다.
- OrbStack Ubuntu: 사용자/그룹, 권한, SSH, UFW, cron, 앱 실행 검증을 담당한다.
- Docker: 이 미션에서는 보조 수단이다. 사용자/cron/systemd 실습은 OrbStack Ubuntu Machine이 더 적합하다.

## 기대 결과

각 단계 문서의 기준을 통과하면 README에 증빙을 정리할 수 있다.

## 결과 확인 명령어

```bash
find . -maxdepth 3 -type f | sort
```

## README에 붙여넣을 증빙

```text
B1-1 미션은 docs/00-목차.md의 순서대로 수행했다.
필수 과제는 01~10단계, 보너스 과제는 11~12단계에서 검증했다.
```

## 자주 발생하는 오류

- macOS에서 `useradd`, `ufw`, `systemctl`을 실행하려고 하는 경우: OrbStack Ubuntu에서 실행해야 한다.
- ZIP을 먼저 무작정 풀고 파일명을 헷갈리는 경우: `unzip -l agent-app.zip`으로 내부 목록을 먼저 확인한다.

## 다음 단계로 넘어가는 기준

macOS와 Ubuntu에서 실행할 명령의 위치를 구분할 수 있으면 다음 단계로 넘어간다.

## Git 커밋 시점

목차와 체크리스트 문서를 처음 추가한 뒤 커밋한다.

## 추천 커밋 메시지

```bash
git commit -m "docs: add B1-1 mission analysis and checklist"
```
