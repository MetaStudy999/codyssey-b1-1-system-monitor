# 전체 검증 체크리스트

## 이 단계의 목표

B1-1 필수 요구사항과 보너스 요구사항을 제출 전 한 번에 점검한다.

## 왜 이 작업을 하는가?

평가자는 기능 구현뿐 아니라 증빙과 설명 가능성을 본다. 명령과 결과를 README에 남겨야 재현 가능하다.

## 사전 확인

Ubuntu에서 앱이 실행 중인지 확인한다.

```bash
ps -ef | grep '[a]gent-app'
ss -tulnp | grep 15034
```

## 실행 명령어

시스템:

```bash
cat /etc/os-release
whoami
id
hostname
ip addr
```

SSH:

```bash
sudo grep -E '^(Port|PermitRootLogin)' /etc/ssh/sshd_config
sudo ss -tulnp | grep ssh
```

방화벽:

```bash
sudo ufw status verbose
sudo ufw status numbered
```

계정/그룹:

```bash
id agent-admin
id agent-dev
id agent-test
getent group agent-common
getent group agent-core
```

권한:

```bash
export AGENT_HOME=/home/agent-admin/agent-app
namei -l "$AGENT_HOME"
sudo ls -ld "$AGENT_HOME"
sudo ls -ld "$AGENT_HOME/upload_files"
sudo ls -ld "$AGENT_HOME/api_keys"
sudo ls -ld "$AGENT_HOME/bin"
sudo ls -ld /var/log/agent-app
sudo getfacl /home/agent-admin
sudo getfacl "$AGENT_HOME/upload_files"
sudo getfacl "$AGENT_HOME/api_keys"
sudo getfacl /var/log/agent-app
```

앱:

```bash
ps -ef | grep '[a]gent-app'
ss -tulnp | grep 15034
curl http://localhost:15034
```

monitor:

```bash
bash -n "$AGENT_HOME/bin/monitor.sh"
sudo -u agent-admin "$AGENT_HOME/bin/monitor.sh"
tail -n 5 /var/log/agent-app/monitor.log
```

cron:

```bash
sudo crontab -u agent-admin -l
wc -l /var/log/agent-app/monitor.log
sleep 70
wc -l /var/log/agent-app/monitor.log
```

logrotate:

```bash
sudo cat /etc/logrotate.d/agent-app-monitor
sudo logrotate -d /etc/logrotate.d/agent-app-monitor
```

보너스:

```bash
bash -n "$AGENT_HOME/bin/report.sh"
sudo -u agent-admin "$AGENT_HOME/bin/report.sh"
bash -n scripts/archive-old-logs.sh
sudo bash scripts/archive-old-logs.sh
find /var/log/monitor/agent-app/archive -type f -name "*.gz" -ls
```

## 명령어 설명

각 명령은 README 증빙에 대응한다. 실패한 명령은 트러블슈팅 문서에 원인과 해결을 기록한다.

## 기대 결과

필수 항목이 모두 완료 상태이고, 보너스 항목도 실행 또는 예외 처리 결과가 확인된다.

## 결과 확인 명령어

```bash
git status
git log --oneline --graph --all --decorate -n 20
```

## README에 붙여넣을 증빙

각 항목별 핵심 출력만 README에 정리한다. 전체 원본은 `docs/verification-log.md` 또는 `docs/command-log.md`에 남긴다.

## 자주 발생하는 오류

- 증빙 누락: 명령은 성공했지만 README에 결과가 없는 경우 평가에서 불리하다.
- root로 앱 실행: 반드시 `agent-admin` 실행 증빙을 남긴다.
- cron 확인 시간 부족: 최소 70초 기다린 뒤 라인 수를 비교한다.

## 다음 단계로 넘어가는 기준

README 요구사항 반영표가 모두 완료 또는 사유 있는 미완료로 정리되면 제출 준비가 가능하다.

## Git 커밋 시점

최종 체크리스트와 검증 문서를 정리한 뒤 커밋한다.

## 추천 커밋 메시지

```bash
git commit -m "docs: align B1-1 evidence with evaluation checklist"
```
